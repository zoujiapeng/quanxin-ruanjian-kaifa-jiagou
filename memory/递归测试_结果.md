---
name: 递归测试 001 结果
description: 递归测试概念验证成功 — 新 Claude 启动、收到 prompt、调用 Lobster
type: project
originSessionId: 25f91506-1287-4d6c-939b-7cdc50057393
---
## 2026-05-03 递归测试 001 结果

### 成功 ✅
1. Windows Terminal 成功打开（非 cmd.exe）
2. 新 Claude Code 实例成功启动（`claude` 交互模式）
3. 新 Claude 收到中文 prompt，理解了 GUI 自动化任务
4. 新 Claude 主动发现并尝试调用 Lobster（先试了 `lobster.py list`，发现关联了 PyCharm，改用了 `python lobster.py list`）

### 阻塞 ❌
1. **权限审批** — 新 Claude 执行 `python lobster.py list` 时卡在 Bash 权限提示，需要用户在终端里选 "Yes"
2. **lobster.py 文件关联** — Windows 把 `.py` 关联到了 PyCharm/Java，直接 `lobster.py` 执行有问题，必须用 `python lobster.py`
3. **CLI 优先于 MCP** — 新 Claude 选择了 CLI 命令（`lobster.py call`）而不是 MCP 工具（`lobster_*`），可能是因为 prompt 中同时提到了两种方式

### 下一步
- 用户需要在那个终端窗口里批准 "python ... lobster.py list" 命令
- 之后的新命令会继续需要权限审批（每次都要 Yes）
- 长期需要：预设权限 allowlist 或改用纯 MCP 工具流程
