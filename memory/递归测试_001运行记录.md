---
name: 递归测试 001 运行记录
description: 首次递归测试执行情况 — 终端启动成功，存在焦点/窗口管理问题
type: project
originSessionId: 25f91506-1287-4d6c-939b-7cdc50057393
---
## 测试执行情况

**2026-05-03** 首次执行递归测试 001：

### 成功点
- 新终端窗口成功从 `claude1` 目录启动
- Python 脚本开始执行，pipe prompt 到 `claude -p`
- 新的 Claude 实例应该收到了 prompt

### 问题
1. **焦点丢失** — 用户移动鼠标后新终端失去焦点，但这对 piped claude 不影响（stdin 已经连接）
2. **全屏遮挡** — 新终端窗口全屏打开遮挡了当前 Claude Code 窗口
3. **自动化操作的焦点依赖** — 后续 lobster type_text/click_target 等操作依赖目标窗口（如记事本）处于焦点状态

### 改进方向
- 终端窗口应更小、不遮挡主窗口
- 自动化流程中需要确保 `focus_window` 调用在 type/click 之前
- 或改用后台 `run_command` 执行而非新窗口（不遮挡但看不到输出）
